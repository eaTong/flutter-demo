name: flutterdemo
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fluro: ^1.6.0
  cupertino_icons: ^0.1.2
  dio: ^3.0.9
  dio_cookie_manager: ^1.0.0
  bot_toast: ^2.4.0
  flutter_form_builder: ^3.8.2
  flutter_slidable: "^0.5.4"
  mobx: ^1.1.1
  flutter_mobx: ^1.1.0
  mobx_codegen: ^1.0.3
  flutter_mobx_forms: ^1.0.0+1
  pull_to_refresh: ^1.5.7

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true